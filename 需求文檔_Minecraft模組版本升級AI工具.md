# 需求文檔：Minecraft 模組版本升級 AI 工具

| 欄位 | 內容 |
| --- | --- |
| 文檔名稱 | Minecraft 模組版本升級 AI 工具 需求規格說明書 |
| 版本 | v0.1（初稿） |
| 日期 | 2026-08-15 |
| 狀態 | 待評審 |
| 作者 | 需求分析員 |

---

## 1. 背景與動機

Minecraft 每次大版本更新（如 1.20.1 → 26.3）都會帶來大量 API 破壞性變更：註冊表改動、mapping 重命名、事件系統變更、資料包格式更新等。模組作者必須人工查閱新版本文件、逐一修改程式碼，過程繁瑣、重複且容易遺漏。

本工具是一個**專用於升級模組 Minecraft 版本**的 AI 工具：透過「版本環境文檔」給 AI 提供新版本的正確知識，配合 Agent 自動分析並修改模組原始碼，將模組從舊版本遷移到目標版本，同時透過 Skills 體系約束 AI 行為（不可破壞模組本身功能等）。

## 2. 目標

- **G1** 在無人工介入或最少人工介入下，將模組原始碼從舊 Minecraft 版本遷移到目標版本。
- **G2** 遷移過程不破壞模組原有功能與行為（在不變更需求的層面上）。
- **G3** 產出可供開發者檢視、審查、手動修正的遷移報告與差異（diff）。
- **G4** 支援透過外部 Skills 與內建 Skills 控制 AI 的升級行為與規則。
- **G5** 模型接入完全由使用者自備 API Key，工具不綁定任何特定廠商。

## 3. 非目標（Non-Goals）

- 不取代模組作者對「功能設計」的決策——AI 只做版本遷移，不做功能重設計。
- 不保證語意完全一致——破壞性行為變更由新版本決定，AI 僅盡力轉譯並標註風險。
- 不做編譯建構以外的自動測試（如遊戲內冒煙測試）——如需要由使用者自行驗證或後續擴充。
- 不做伺服器端託管或多人協作平台功能。

---

## 4. 總體架構

```
┌─────────────────────────────────────────────────────┐
│                    Agent 升級工具                      │
│  ┌───────────┐   ┌─────────────┐   ┌─────────────┐  │
│  │ 規劃階段   │ → │ 遷移執行階段 │ → │ 驗證與報告   │  │
│  └───────────┘   └─────────────┘   └─────────────┘  │
│        │                │                 │          │
└────────┼────────────────┼─────────────────┼──────────┘
         ▼                ▼                 ▼
  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
  │  MC版本環境文檔 │ │  LLM API 接口 │ │  Skills 體系  │
  │ (每版本一份)   │ │ (使用者自備Key)│ │ (外接+內建)   │
  └──────────────┘ └──────────────┘ └──────────────┘
```

### 四大部件職責

| 部件 | 職責 |
| --- | --- |
| **MC 版本環境文檔** | 以結構化文件描述「目標版本」的環境，供 AI 在遷移前載入，作為正確知識來源 |
| **Agent 升級工具** | 遷移主體：規劃 → 執行 → 驗證，呼叫 LLM API 完成程式碼修改 |
| **LLM API 接口** | 統一模型接入抽象層，使用者自備 API Key，可替換不同模型 |
| **Skills 體系** | 提供 AI 可調用的技能與規則（外接 Skill + 內建升級規則），約束與輔助遷移行為 |

---

## 5. 功能需求

### 5.1 MC 版本環境文檔

#### ENV-1 文檔結構

每個 Minecraft 版本（如 `26.3`）有一份獨立環境文檔，涵蓋：

- **版本基本資訊**：版本號、類型（正式版/快照）、發布日期、支援的 Mod Loader（Fabric / Forge / NeoForge）與其版本。
- **mapping 變更**：官方 mapping / intermediary / 自訂 mapping 的類別、方法、欄位重命名對照。
- **註冊表變更**：新增/移除/改名的註冊表條目（物品、方塊、附魔、生物群系等）。
- **API 破壞性變更**：被移除、改名、改簽名的 API，及官方建議的替代方式。
- **事件系統變更**：事件名稱、參數、觸發時機的變化。
- **資料格式變更**：資料包、配方、進度、JSON 結構格式的變化。
- **建構環境變更**：Gradle 設定、mappings 依賴、loom/ForgeGradle 版本、Java 版本要求。
- **已知遷移陷阱**：社群/官方已知的常見踩坑點。

- **優先級**：P0
- **驗收標準**：
  - [ ] 至少涵蓋上列 8 個小節，各小節內容可被 AI 直接讀取引用。
  - [ ] 文檔以 Markdown 結構化撰寫，且可直接由 Agent 的 Skill 讀入。

#### ENV-2 版本對照（來源版本 → 目標版本）

- **描述**：除單一目標版本的環境文檔外，提供「來源版本 → 目標版本」的差異對照段，聚焦本遷移路徑真正會碰到的變更，減少 AI 閱讀量與幻覺。
- **優先級**：P1
- **驗收標準**：
  - [ ] 對 1.20.1 → 26.3 路徑，能產出直接針對該路徑的變更摘要。

#### ENV-3 文檔品質與維護

- **描述**：文檔必須可讀、可維護、可更新：
  - 由人工專家撰寫初版，來源標註官方 changelog / 文件 / 社群 wiki。
  - 支援版本化（隨目標 MC 版本命名），新版本發布時可增量更新。
- **優先級**：P0
- **驗收標準**：
  - [ ] 每份文檔標註資料來源與最後更新時間。
  - [ ] 文檔更新不影響已存在之其他版本文檔。

---

### 5.2 Agent 升級工具

#### AGENT-1 遷移流程

Agent 依下列階段執行遷移：

1. **載入環境**：讀取目標 MC 版本環境文檔（ENV-1/ENV-2）與內建升級規則 Skill。
2. **分析來源**：掃描模組專案結構、`build.gradle` / `gradle.properties`、主要 package、使用的 Loader API。
3. **規劃遷移**：產生遷移計畫（修改哪些檔案、變更哪些 API、預期風險）。
4. **執行遷移**：依計畫逐檔修改，呼叫 LLM API 產生新程式碼。
5. **建構驗證**：嘗試編譯（如專案支援），收集錯誤並回饋給 LLM 進行修正迭代。
6. **產出報告**：輸出遷移報告與 diff。

- **優先級**：P0
- **驗收標準**：
  - [ ] 對一個樣本模組（1.20.1，使用已變更 API）執行完整流程，輸出可審查的改動與報告。

#### AGENT-2 遷移報告與 Diff

- **描述**：報告必須人類可讀，包含：
  - 修改檔案清單與統計。
  - 每個變更的說明（原 API → 新 API）。
  - 需要人工確認的「不確定/有風險」變更清單。
  - 完整 git diff 或等同格式。
- **優先級**：P0
- **驗收標準**：
  - [ ] 報告同時輸出 Markdown 摘要與 patch/diff 檔。

#### AGENT-3 迭代修正（錯誤回饋迴路）

- **描述**：編譯失敗或驗證失敗時，Agent 將錯誤訊息回饋給 LLM，修正後重試，直到通過或達到最大迭代次數（可設定，預設 5 次）。
- **優先級**：P0
- **驗收標準**：
  - [ ] 可設定最大迭代次數；超過時停止並在報告標示未解決問題。

#### AGENT-4 保留原功能

- **描述**：遷移不得改變模組的「公開行為/設定/資源」語意；AI 僅做版本遷移轉譯。此為內建 Skills 的核心約束（見 SKILL-2）。
- **優先級**：P0
- **驗收標準**：
  - [ ] 遷移後的設定檔、資源檔內容在語意上與遷移前一致（除必要的格式遷移）。

#### AGENT-5 斷點與可恢復性

- **描述**：遷移中途失敗/中斷時，保留已完成的修改與狀態，可從斷點續跑，不重做全部工作。
- **優先級**：P1
- **驗收標準**：
  - [ ] 模擬中斷後重啟，已完成的檔案不被重複覆蓋且能接續。

#### AGENT-6 多人操作與安全（對原專案）

- **描述**：工具不得在未告知下覆寫使用者未修改區域；預設在**工作副本/分支**上執行，而非直接改動主分支。
- **優先級**：P0
- **驗收標準**：
  - [ ] 執行前確認目前 git 狀態乾淨或提示使用者確認。
  - [ ] 提供 `--dry-run`（只出計畫，不改檔案）。

---

### 5.3 LLM API 接口

#### LLM-1 統一抽象

- **描述**：提供統一接口封裝不同模型供應商（OpenAI、Anthropic、Google、DeepSeek、本地 Ollama 等），支援 Chat / 補全、與 Tool/Skill 呼叫能力。
- **優先級**：P0
- **驗收標準**：
  - [ ] 更換供應商只需改設定，不修改 Agent 業務程式碼。

#### LLM-2 自備 API Key

- **描述**：API Key 由使用者提供並存於本機（環境變數或本機設定檔），**不**隨專案提交、不經第三方伺服器轉發。
- **優先級**：P0
- **驗收標準**：
  - [ ] 設定檔被 `.gitignore` 排除；程式碼不含硬編碼 Key。

#### LLM-3 錯誤與重試

- **描述**：處理限流（429）、逾時、網路錯誤、模型不存在等錯誤，並支援指數退避重試。
- **優先級**：P0
- **驗收標準**：
  - [ ] 模擬 429 與逾時時能重試成功或明確報錯。

#### LLM-4 上下文管理

- **描述**：管理 token 預算，避免長文件爆掉模型上下文；必要時分塊、摘要或只送相關段落（如只送被改檔案與相關環境文檔章節）。
- **優先級**：P0
- **驗收標準**：
  - [ ] 對大型模組（>50 個檔案）仍能穩定完成遷移而不超過上下文上限。

---

### 5.4 Skills 體系

#### SKILL-1 外接 Skills 載入

- **描述**：支援載入外部 Skills（遵循業界通用 Skill 目錄格式），並可與內建 Skills 共存。
- **優先級**：P1
- **驗收標準**：
  - [ ] 放入外接 Skill 後可被 Agent 偵測並呼叫。

#### SKILL-2 內建升級規則 Skill

- **描述**：內建核心規則，至少包含：
  - 不可破壞模組本身的公開功能與設定語意。
  - 只做版本遷移轉譯，不進行需求性功能新增。
  - 保留註解、命名風格、程式碼風格與既有結構（除非新版 API 必要）。
  - 遇到不確定變更時標註為「需人工確認」，不得靜默猜測。
  - 使用目標版本的 mapping 與 API，不可混用舊版 API。
- **優先級**：P0
- **驗收標準**：
  - [ ] 每個規則獨立、可讀、可更新；違反規則時 Agent 會自我修正或標記。

#### SKILL-3 Skill 可讀性與維護

- **描述**：內建 Skills 以易讀 Markdown 撰寫，集中管理（如 `skills/` 目錄），每個 Skill 含名稱、用途、適用時機、內容、使用限制。
- **優先級**：P0
- **驗收標準**：
  - [ ] 新增/修改內建規則不需改動 Agent 主程式。

---

## 6. 非功能需求

### 6.1 可讀性

- 報告、遷移計畫、環境文檔、Skills 皆為結構化、人類可讀格式（Markdown）。
- 報告需含「需人工確認」清單，方便開發者快速審查。

### 6.2 可維護性

- 四大部件低耦合：環境文檔資料驅動、LLM 接口抽象、Skills 外置、Agent 只做流程編排。
- 新增 MC 版本＝新增環境文檔，不改主程式。
- 設定集中於單一設定檔，提供預設值與註解。

### 6.3 安全性

- **API Key**：僅存本機、git 排除、絕不寫入 log。
- **程式執行**：建構/驗證指令在可控環境執行，不執行來自模型輸出的任意 shell 指令（模型只改檔案，不直接觸發建構之外的指令）。
- **檔案安全**：寫入前備份/確認；`--dry-run` 不寫任何檔案。
- **程式碼審查面**：AI 產生的程式碼不直接注入使用者環境，僅作為檔內修改並可被 diff 審查。

### 6.4 效能

- 對大型模組：上下文管理（LLM-4）避免 token 浪費；只處理受影響檔案。
- 迭代修正（AGENT-3）設上限，避免失控成本。
- 提供 Token/成本估算與使用量統計（可選）。

### 6.5 錯誤處理

- 所有外部呼叫（LLM、建構、檔案 IO）皆有明確錯誤分類與使用者可讀訊息。
- 失敗不靜默：產出「失敗摘要 + 已改檔案 + 建議人工處理方式」。
- 支援中斷續跑（AGENT-5）與 `--dry-run` 預覽。

---

## 7. 使用案例（情境）

| 編號 | 情境 | 預期結果 |
| --- | --- | --- |
| UC-1 | 將模組 A（1.20.1）升級到 26.3 | 自動完成遷移並產出報告與 diff |
| UC-2 | 遷移中遇到不確定 API 變更 | 該處標註「需人工確認」，其餘繼續 |
| UC-3 | 編譯失敗 | Agent 讀取錯誤回饋並修正，最多 N 次 |
| UC-4 | 使用者只想預覽 | `--dry-run` 只出遷移計畫與預估變更，不寫檔 |
| UC-5 | 使用者更換模型供應商 | 僅改設定即可切換，流程不變 |
| UC-6 | 遷移中途斷線/中斷 | 重啟後從斷點續跑，不重做已完成檔案 |
| UC-7 | 使用者加入自訂升級規則 | 放入外接 Skill 後即被採用 |

## 8. 里程碑（建議）

| 里程碑 | 內容 | 對應需求 |
| --- | --- | --- |
| M0 | 版本環境文檔模板 + 1 份範本文檔（1.20.1 → 26.3） | ENV-1、ENV-2 |
| M1 | LLM API 接口抽象 + API Key 管理 | LLM-1、LLM-2 |
| M2 | Agent 流程（分析→規劃→執行→報告） | AGENT-1、AGENT-2、AGENT-6 |
| M3 | 建構驗證 + 迭代修正迴路 | AGENT-3、AGENT-5 |
| M4 | Skills 體系（外接 + 內建規則） | SKILL-1、SKILL-2、SKILL-3 |
| M5 | 大型模組上下文管理 + 效能/成本統計 | LLM-4、6.4 |

## 9. 驗收準則（總結）

1. 對一個樣本模組完成 1.20.1 → 26.3 的遷移，產出可審查 diff 與報告（AGENT-1/2）。
2. 遷移後設定語意與資源內容未被破壞（AGENT-4、SKILL-2）。
3. 所有模型供應商透過設定可切換，Key 不落庫不洩漏（LLM-2）。
4. 大型模組在上下文上限內完成遷移（LLM-4）。
5. 中斷可續跑、`--dry-run` 不寫檔、失敗有明確訊息（AGENT-5/6、6.5）。

## 10. 風險與開放問題

| 風險／問題 | 說明 | 影響 | 因應 |
| --- | --- | --- | --- |
| 環境文檔不完整 | 文檔涵蓋不全導致 AI 猜測 | 高 | 權威來源人工維護 + 標註資料來源；不確定處標註需人工確認 |
| AI 幻覺產生錯誤 API 用法 | 即使有文檔仍可能誤用 | 高 | 建構驗證迴路 + 迭代修正 + 風險標註 |
| 大型模組成本 | 檔案多、迭代多，token 成本高 | 中 | 上下文管理 + 只處理受影響檔案 + 迭代上限 + 成本統計 |
| 語意行為偏移 | 遷移後行為與新版不同 | 中 | 報告標註風險；遊戲內驗證列為後續擴充 |
| 建構環境差異 | 樣本專案無法本機編譯 | 中 | 建構驗證為可選步驟，失敗時退回首程式碼審查 |
| 開放問題 | 是否支援多個 Loader（Fabric/Forge/NeoForge）同時遷移？ | - | 需確認優先支援哪個 Loader 生態 |

## 11. 附錄：名詞表

| 名詞 | 說明 |
| --- | --- |
| MC 版本環境文檔 | 描述特定 Minecraft 版本開發環境（mapping、API、事件、格式變更）的結構化文件 |
| Mod Loader | 模組載入器：Fabric、Forge、NeoForge 等 |
| Mapping | 混淆代碼到可讀名稱的對應表 |
| Agent | 執行遷移流程的 AI 代理程式 |
| Skill | 供 AI 呼叫的技能/規則包（外接或內建） |
| 迭代修正 | 編譯/驗證失敗後回饋錯誤、由 LLM 重新修改的重試迴路 |
| 需人工確認 | 遷移中不確定、需開發者介入決策的變更標記 |
